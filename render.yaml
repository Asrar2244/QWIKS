services:
  - type: web
    name: qr-menu-frontend
    env: static
    rootDir: frontend
    buildCommand: npm install && npm run build:render
    staticPublishPath: ./build
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: CI
        value: false
